# Development Log - 2026-06-18

## Completed

- Implemented the first lightweight State-First Life Simulation layer for continuity-critical story threads.
- Added `worldState.storyState` to new runs with facts, closed facts, thread stages, active pressures, and forbidden repeat scene skeletons.
- Added `src/action-intent.js`, `src/simulation-kernel.js`, `src/story-state.js`, `src/narrative-director.js`, and `src/story-contract-validator.js`.
- Wired player choice/free-form resolution so the engine records authoritative story facts before the next life event is generated.
- Wired mock and provider life-event generation to accept an event contract. Mock fallback now consumes the same contract instead of hardcoding the repeated cultivation "forest jade object + footsteps" scene.
- Added regression coverage in `tests/state-first-story.test.mjs` for the jade-talisman/lingyinfu repetition bug: the engine closes the first-discovery fact, directs the next family-pressure event, rejects repeated discovery skeletons, and verifies play-session integration.
- Restored provider fallback behavior in `play-session.js` so failed real-AI calls still fall back to local safe generation with `provider_fallback_*` flags while passing the same validation path.
- Updated technical and MVP skeleton docs to record the new state-first story-state/director/contract layer.
- Updated `README.md` and `docs/README.md` so the state-first continuity layer is discoverable from the project entry points.
- Added an annual state transition layer for repeated yearly event shapes. The new annual fact package forces a concrete yearly life delta before AI/mock rendering when the same scene structure starts to dominate multiple age nodes.
- Expanded the annual layer into a default Year Tick director for cross-year branches. It now detects stale yearly shapes across family, education, social, institution, resource, health, relationship, route, and world-pressure domains, keeps ongoing threads as background, and writes annual facts back to `worldState.storyState`.

## Decisions

- AI prose is no longer treated as the authority for continuity-critical story facts. The engine records closed facts and thread stage in `worldState.storyState`; AI and mock providers render within the next event contract.
- The MVP implementation stays intentionally narrow: it protects the cultivation jade-talisman/lingyinfu repeated-discovery loop first, while leaving broader thread extraction for future expansion.
- Provider failures should keep the playtest session alive with marked local fallback, matching the existing README/technical-standard promise. Fallback events must still honor story contracts.
- Repeated yearly scene structures are a separate class of continuity failure from repeated facts. They should be handled by an annual fact package and forbidden event-shape validation, not by hardcoding cooldowns for one named plotline.
- The annual fact package is now the default authority for what a cross-year branch is about. A long-running thread may continue, but it cannot consume every year as the main event unless the Year Tick selects it as the primary life delta.

## Todo

- Generalize the state-first thread catalog beyond the jade-talisman case so Cthulhu clues, wasteland resources, factions, and NPC secrets can use the same lifecycle protection.
- Add richer provider repair instructions for event-contract violations, so real AI can rewrite contract-breaking prose before falling back.
- Consider save migration/versioning once `storyState` grows beyond the current backward-compatible optional shape.

## Verification

- `npm test` passed.
- `npm run validate:data` passed.
- `npm run smoke:web` passed in mock mode.
