# Development Log - 2026-06-19

## Completed

- Added the Event-Sourced Life Runtime final MVP layer: accepted `statePatch` proposals now convert into DomainEvents, pass through `transitionRun()`, reduce into a run projection, and append to `run.eventLog`.
- Added `src/runtime/event-log.js`, `src/runtime/transition-run.js`, `src/runtime/replay-run.js`, `src/runtime/invariants.js`, `src/domain/events/*`, `src/domain/reducers/run-reducer.js`, and PlayerView/PromptView/GMView projections.
- Rewired `applyAiResponseToRun()`, save/load, AI prompt snapshots, web session serialization, and GM dev tools around the event-sourced runtime.
- Added Replay Lab coverage with `tests/replay-fixtures/jade-thread-event-log.json`, `tools/replay-bugs.mjs`, and `npm run replay:bugs`.
- Added architecture guard coverage with `tests/runtime-architecture.test.mjs`, `tools/test-architecture.mjs`, and `npm run test:architecture`.
- Hardened ordinary PlayerView into label-first attribute and relationship arrays so ordinary UI data does not structurally depend on backend attribute or NPC relationship keys.
- Updated README, technical standards, MVP skeleton, AI output protocol, and documentation index for the event-sourced runtime.

## Decisions

- Current saves treat `run.eventLog` as the replay authority when present. The run snapshot remains useful for compatibility and debugging, but load should rebuild from the event log first.
- DomainEvents are engine-owned. AI, mock, UI, and GM tools may propose changes, but reducers are the only code path allowed to settle those changes into the run projection.
- PlayerView, PromptView, and GMView are separate projections for different audiences. Ordinary player data should migrate toward PlayerView; GM/debug may keep full run and hidden fields.
- The MVP keeps legacy `run` fields available for current web compatibility while establishing PlayerView as the ordinary-player projection boundary.

## Todo

- Expand Replay Lab fixtures beyond the current jade-thread regression so every severe continuity/state bug is reproducible from an event log.
- Continue moving ordinary web rendering from legacy compatibility `run` fields to PlayerView-first data, while keeping GM/debug access through GMView.
- Add event-log versioning/migration rules once DomainEvent payloads change across releases.

## Verification

- `npm test` passed.
- `npm run validate:data` passed.
- `npm run replay:bugs` passed.
- `npm run test:architecture` passed.
- `npm run smoke:web` passed in mock mode.
- `git diff --check` passed after trimming a trailing blank line.
