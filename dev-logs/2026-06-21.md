# 2026-06-21

## Completed

- Added a persisted Spec-to-Proof contract for the Yearly Outcome Ledger v1 repair.
- Added death tests for the real annual user/session path:
  - a started run generating the next annual event must record `annual.outcome_recorded` and `growth.evidence_added`;
  - ordinary web save/load and opening-advance flows must show Growth Ledger changes through `panelViews.attributes`;
  - replay from `eventLog` must preserve yearly outcomes and panel values.
- Replaced direct annual generation paths in `src/play-session.js` with contracted annual generation:
  - `createPlayableSession()`;
  - `createPlayableSessionAsync()`;
  - `advanceOpeningSync()`;
  - `advanceOpeningAsync()`.
- Confirmed the old direct annual path failed before the fix and the new death tests pass after the fix.

## Follow-Up

- This repair does not change annual director selection, curriculum selection, old story asset budgets, opening variation, or attribute type semantics.
