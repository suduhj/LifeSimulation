# 2026-06-21 Development Log

## Completed

- Added the mandatory Spec-to-Proof Operating System project rule.
- Added `docs/spec-to-proof-operating-system.md` as the complete reference for Proof Contracts, death tests, old-path replacement, and evidence requirements.
- Updated `GPT.md` so agents must use Proof Contracts before non-trivial fixes, architecture changes, AI-output changes, UI-visible changes, persistence/replay changes, state synchronization changes, or repeated bugs.
- Updated `docs/technical-standards.md` with the architecture rule that new abstractions must replace, disable, migrate, isolate, runtime reject, or test-block old user-impacting paths.
- Updated `docs/execution-plan.md` with the required Spec-to-Proof execution gate.
- Implemented the Dual-Surface / Player Surface runtime after Proof Contract confirmation.
- Added `mvp.player_view.v1` as the ordinary browser API surface. Normal `startRun`, `submitAction`, `loadSession`, and `getPlayerView` responses now return only `sessionId` plus safe `playerView`.
- Added Player Surface projection, validation, storage, and event-sourced recording through `player_surface.view_recorded` / `player_surface.rejected`.
- Updated ordinary web rendering to use `playerView` and kept raw run/current-event serialization on dev/GM paths only.
- Added death tests proving ordinary payloads do not expose raw session fields and that polluted old-path data cannot affect PlayerView.
- Added a death test proving canonical backend IDs such as `mentor_attention` are masked before reaching PlayerView source/node identifiers.
- Updated `smoke:web` to verify the real ordinary PlayerView entry point instead of the old raw `run/currentEvent` response path.
- Repaired the fixed 0-6 opening timeline path under a dedicated Proof Contract.
- Added death tests for ordinary `PlayerView.timeline` proving fixed opening template titles are not shown, same-world different setups vary, high/low Family Background age-0 bodies differ, three worlds differ, and polluted legacy opening fallback cannot reach ordinary PlayerView/web timeline.
- Migrated opening origin ledger nodes into per-age `opening_year` LifeNodes during statePatch-to-DomainEvent conversion so ordinary timelines use `LifeNode -> PlayerView` instead of legacy `opening.earlyLifeTimeline` or web fallback templates.
- Disabled the ordinary web opening fallback path; legacy opening timeline helpers are debug-only/runtime rejected and test-blocked from the normal player entry point.
- Updated opening timeline docs so `PlayerView.timeline` / per-age LifeNodes are the ordinary authority and legacy opening data is backend/debug compatibility state only.
- Repaired Annual Year Tick v2 under a dedicated Proof Contract.
- Replaced the real annual play-session path (`createPlayableSession`, async playable sessions, and first branch after opening) so annual life events go through `buildNextEventContract -> assertStoryContract -> applyAnnualFactPackageToResponse` instead of raw mock/provider `playerText`.
- Added death tests proving the real annual session path records curriculum slots, topic profiles, annual agendas, annual LifeNodes, and domain events before ordinary PlayerView renders the year.
- Added death tests proving ages 5-10 cover at least four curriculum slots through the real session path and that the same curriculum slot does not repeat consecutively.
- Made Topic Ledger selection active, not advisory: if the selected annual topic repeats a recent topic family, arena, or object focus, the annual focus chooser moves to another curriculum/topic candidate.
- Disabled `eventHistory` as an ordinary story timeline fallback; ordinary PlayerView timeline now projects from LifeNodes, while raw event history remains backend/debug compatibility data only.
- Repaired the Yearly Outcome Ledger v1 annual user path under a dedicated Proof Contract.
- Added death tests for the real annual user/session path:
  - a started run generating the next annual event must record `annual.outcome_recorded` and `growth.evidence_added`;
  - ordinary web save/load and opening-advance flows must show Growth Ledger changes through `playerView.panels.attributes`;
  - replay from `eventLog` must preserve yearly outcomes and panel values.
- Replaced direct annual generation paths in `src/play-session.js` with contracted annual generation:
  - `createPlayableSession()`;
  - `createPlayableSessionAsync()`;
  - `advanceOpeningSync()`;
  - `advanceOpeningAsync()`.
- Confirmed the old direct annual path failed before the fix and the new death tests pass after the fix.

## Decisions

- During Proof Contract phase, only read-only inspection is allowed.
- Implementation may begin only after the user explicitly says `确认 Proof Contract，开始实现`.
- Completion requires proof that the old wrong behavior cannot affect the actual user entry point, not just proof that a new module or abstraction exists.
- Ordinary browser UI authority is now PlayerView / Player Surface, not raw `run`, `currentEvent`, `playerText`, `statePatch`, legacy `playerContract`, or direct `panelViews`.
- Canonical IDs may remain in event log, LifeNodes, reducers, and GM/debug tools, but PlayerView must expose only public opaque source/node IDs.

## Remaining Todo

- Full old-asset sentence budgets remain separate follow-up work.
- Annual/growth balance remains separate follow-up work.
- This Yearly Outcome repair does not change annual director selection, curriculum selection, old story asset budgets, opening variation, or attribute type semantics.
